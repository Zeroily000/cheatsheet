{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Example (Debug)",
            "type": "shell",
            "command": "bazel build :example_test -c dbg",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}